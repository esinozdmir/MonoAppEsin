class RegisterViewmodel {
  static void openTermsPage() {
    // Kullanım Koşulları sayfasını açan kod buraya gelecek
    // Örneğin: Navigator.push(context, MaterialPageRoute(builder: (context) => TermsPage()));
    print("Kullanım Koşulları sayfası açıldı.");
  }

  static void openUserAgreementPage() {
    // Kullanıcı Sözleşmesi sayfasını açan kod buraya gelecek
    // Örneğin: Navigator.push(context, MaterialPageRoute(builder: (context) => UserAgreementPage()));
    print("Kullanıcı Sözleşmesi sayfası açıldı.");
  }

  static void clarificationPage() {
    // Kullanıcı Sözleşmesi sayfasını açan kod buraya gelecek
    // Örneğin: Navigator.push(context, MaterialPageRoute(builder: (context) => UserAgreementPage()));
    print("Aydınlatma metni açıldı.");
  }
}
